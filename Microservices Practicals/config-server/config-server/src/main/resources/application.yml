server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Irushinie/spring-cloud-config-store
          default-label: main
          search-paths:
            - '*service'
